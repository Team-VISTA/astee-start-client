import AuthModal from '../layout/AuthModal';
import Layout from '../layout/Layout';

const Projects = () => {
  return (
    <Layout>
      Projects
      <AuthModal />
    </Layout>
  );
};

export default Projects;
